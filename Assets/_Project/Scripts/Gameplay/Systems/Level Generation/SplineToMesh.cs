using UnityEngine;

namespace Smoove.Gameplay.Systems.LevelGeneration
{
    [RequireComponent(typeof(MeshFilter), typeof(MeshRenderer))]
    public class SplineToMesh : MonoBehaviour
    {
        private void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
